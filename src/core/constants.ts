export const DEFAULT_PREVIEW_BYTES = 256 * 1024;

export const DEFAULT_RANGE_PROBE_BYTES = 256 * 1024;

export const DEFAULT_RANGE_MAX_PROBE_BYTES = 16 * 1024 * 1024;

export const DEFAULT_MAX_LINE_CHARS = 1024 * 1024;

export const DEFAULT_LINE_OVERLAP_CHARS = 1024;

export const DEFAULT_KEY_BYTES = 32;

export const MASK_TOKEN_PREFIX = 'R';

export const ALWAYS_REDACT_RULE_ID = 'custom';

export const ALWAYS_REDACT_TOKEN = 'CUSTOM';
